# sesion-01b

## apuntes sesión

Compuertas (rellenar)

las variables son muy especificas pero hay que aproximarlo, si no nos hiriamos por decimales y perderiamos tiempo

string = tiene que ver con la variable char, char es character, solo deja una letra

c++ variables

int
double
char
string
bool

hay que descargar arduino IDE, yo no lo descargo porque ya lo tengo.

arduino r4, tiene usb C que viene categorias, minima y wifi

Setup es una secuencia de instruciones para que ocurran cosas

La palabra void es un tipo de funcion, esta funcion ocurre nomas no expulsa un valor por eso se llama vacio 👀

los corchetes signifca darle inicio y fin 

parentesis = a que es una funcion

// = es para realizar comentarios sin necesidad de programar

loop es para que se repita mucho

if 

if condicionales

10 millones de colores, con 244 bits tengo ma de 10 millones de calores posibles
receptores rojizo, verdoso, azuloso.
entonces R de rojo tiene 8 bits G de verde tb, B de azul tb.

entonces 0 es 0 y 255 prendido

disco duro 2 mb tiene 2 megabytes que equivalen a kilobytes y de kilo pasan a bytes

sistema EXAdecimal que hace que valorers numericos que ocupn 2 numeros pasan a 1, por ejemplo de 01 a 1, de 19 a 15 pasan a letras de A hasta F

como resultado si me gusta el rojo seria ff0000 ff:R 00:G 00:B

funciones tienen (), los nomres tienen que ir junto y la primera letra con otra paalabra en mayuscula: HolaQue

```
// tomas se encuentra en su casa?
bool tomasEnCasa = false;
bool tomasMapuche = true;

//integers
int tomasEdad = 22
int tomasNacimientoAnho = 2004
int tomasNacimientoMes = 3
int tomasNacimientodia = 26
int dientes = 23
int mes actual =
void setup() {
  // put your setup code here, to run once:

}

void loop() {
  // put your main code here, to run repeatedly:
// para celebrar cumpleaños
if mesActual == tomasNacimientoMes {

}
if diaActual == tomasNacimientoDia {

void cumplisAnhosTomas{
  tomasEdad = tomasEdad + 1;
  
}
}

}
```
```
int sumarEnteros (int x, int y) {
int rresultado = 0
}

resultado = X + y

return resutltado

void loop(){
int valor desayuno = sumar (ValorPancito, valorCafecito)
}

```


## encargos

encargo01b:

1. tratar de correr un código en el microcontrolador asignado a cada dupla, incluir referentes, citas, comentarios, imágenes, descripciones textuales, y en caso de éxito o fracaso incluir aciertos, preguntas, dramas, atados. recordatorio que estos apuntes son personales, cada persona sube su versión.
2. proponer una función con nombre, tipo, argumentos y uso, que modele algún área de su interés, por ejemplo subirCerro(enBicicleta), tomarMetro(conPaseEscolar), etc. escribir en pseudocódigo los pasos que necesita esa función internamente para que literalmente funcione.

## lectura
